import { NgxFormatPipe } from './ngx-format.pipe';

describe('NgxFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NgxFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
